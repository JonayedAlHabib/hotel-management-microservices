# Each backend service directly for now — collapses into one gateway URL once api-gateway exists
VITE_API_URL=http://localhost:4001
VITE_BOOKING_API_URL=http://localhost:4002
VITE_PAYMENT_API_URL=http://localhost:4003
